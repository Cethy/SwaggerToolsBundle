security:
    access_control:
{% for accessControl in accessControls %}
        - { path: {{ accessControl.path }},     methods: [{{ accessControl.method }}],       roles: [{{ accessControl.roles|join(', ') }}] }
{% endfor %}