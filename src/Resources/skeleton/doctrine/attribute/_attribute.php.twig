
    /**
     * @var {{ type }}
     * @Type("{{ fqType }}")
     *
     * @ORM\Column(name="{{ ormName }}", type="{{ ormType }}"{% if property.required is not defined or property.required == 'false' %}, nullable=true{% endif %})
     *
{% include 'doctrine/_asserts.php.twig' %}
     */
    private ${{ name }};