
    /**
     * @var {{ type }}
     * @Type("{{ fqType }}")
     *
     * @ORM\OneToMany(targetEntity="{{ ormName }}", mappedBy="{{ ormMappedBy }}", cascade={"persist", "remove"}, orphanRemoval=true)
     *
{% include 'doctrine/_asserts.php.twig' %}
     */
    private ${{ name }};